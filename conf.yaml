# Fake Configuration File

server:
  host: ${SERVER_HOST}
  port: ${SERVER_PORT}
  username: ${USERNAME}
  password: ${PASSWORD}

database:
  host: 3dexperience.3ds.com
  port: ${DATABASE_PORT}
  name: ${DATABASE_NAME}
  username: root
  password: password

logging:
  level: ${LOGGING_LEVEL}
  path: ${LOGGING_PATH}

api:
  endpoint: ${API_ENDPOINT}
  timeout: ${API_TIMEOUT}

# For Detection test only
